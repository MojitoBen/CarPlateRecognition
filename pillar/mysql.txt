####一些mysql常用指令

sudo mysql -u root -p #啟動客戶端並用root登入

CREATE DATABASE your_database_name;
SHOW DATABASES;
USE your_database_name;


CREATE TABLE your_table_name (
    id INT AUTO_INCREMENT PRIMARY KEY,
    column1 VARCHAR(255) NOT NULL,
    column2 INT NOT NULL
);
DESCRIBE your_table_name; #查看結構表
INSERT INTO your_table_name (column1, column2) VALUES ('example', 123); #插入指令
SELECT * FROM your_table_name; #查數據

ALTER TABLE epc ADD COLUMN owner_name VARCHAR(100); -- 添加新列到表的末尾
ALTER TABLE epc ADD COLUMN registration_date DATE AFTER car_plate_num;-- 在特定位置插入新列


chmod +x /home/yunning/Documents/pillar_ver.1.1/pillar1.1.sh

[Unit]
Description=Pillar
After=network.target

[Service]
Type=simple
ExecStart=/home/yunning/Documents/pillar_ver.1.1/pillar1.1.sh
Restart=on-failure
User=yunning

[Install]
WantedBy=multi-user.target

source ~/anaconda3/etc/profile.d/conda.sh
conda activate yolov8
cd /home/yunning/Documents/pillar_ver.1.1
python3 main.py


